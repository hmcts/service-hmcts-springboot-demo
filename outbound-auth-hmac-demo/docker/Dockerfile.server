# Build context must be repo root (service-hmcts-springboot-demo)
# so that gradle/ and outbound-auth-hmac-demo/server are available.

FROM eclipse-temurin:25.0.2_10-jdk AS build
WORKDIR /app
COPY gradle /app/gradle
COPY outbound-auth-hmac-demo/server /app/outbound-auth-hmac-demo/server
WORKDIR /app/outbound-auth-hmac-demo/server
RUN ./gradlew bootJar --no-daemon

FROM eclipse-temurin:25.0.2_10-jre
COPY --from=build /app/outbound-auth-hmac-demo/server/build/libs/*.jar app.jar
EXPOSE 8080
ENTRYPOINT ["java", "-jar", "/app.jar"]
