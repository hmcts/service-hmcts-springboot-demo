server:
  port: 8080

spring:
  application:
    name: http-client-demo

# Base URLs for clients
demo-service:
  url: http://localhost:8081

logging:
  level:
    com.example.demo.client: DEBUG
    feign: DEBUG




