name: Tests

on:
  pull_request:
    branches:
      - main

jobs:
  Test-Modules:
    name: Test ${{ matrix.module }}
    strategy:
      matrix:
        module: [ actuator-demo,
                  audit-filter-demo,
                  auth-filter-demo,
                  json-mapper-demo,
                  jwt-token-demo,
                  exception-handling-demo,
                  openapi-client-demo,
          # clock-demo - Not work in pipeline. Oddly
                  postgres-lock,
                  postgres-springboot4,
                  servicebus-retry-demo,
                  servicebus-queue-demo,
                  servicebus-topic-demo ]
    runs-on: ubuntu-latest
    env:
      APIHUB_ORGANISATION: ${{ vars.APIHUB_ORGANISATION }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Module tests
        run: |
          env
          echo "vars.APIHUB_ORGANISATION is set to \"${{ vars.APIHUB_ORGANISATION }}\""
          echo "APIHUB_ORGANISATION is set to \"${APIHUB_ORGANISATION}\""
          cd ${{ matrix.module }}
          pwd
          ./gradlew clean test
          cd ..


