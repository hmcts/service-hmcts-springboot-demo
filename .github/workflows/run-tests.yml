name: CI Build and Publish

on:
  pull_request:
    branches:
      - main

jobs:
  Test:
    runs-on: ubuntu-latest
    steps:
      - name: Actuator-Demo test
        run: |
          cd actuator-demo
          ./gradle clean test
          cd ..

