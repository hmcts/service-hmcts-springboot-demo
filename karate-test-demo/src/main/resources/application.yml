spring:
  application:
    name: karate-test-demo

server:
  port: ${SERVER_PORT:8083}

management:
  endpoints:
    web:
      base-path: /actuator
      exposure:
        include: health, info, prometheus